# [보존] Todo.md 원본 (2026-05-13 ~ 2026-07-13)

> **이 파일은 동결된 보존본이다.** 2026-08-24 변경 이력 통합으로 `Todo.md`는 [`../CHANGELOG.md`](../CHANGELOG.md)의 `Todo` 섹션으로 대체되었다.
> 현행 할일은 이 파일이 아니라 CHANGELOG의 `Todo` 를 볼 것. 이하 내용은 통합 시점 원문 그대로.
> 참고: 본문이 가리키는 `GIT_RULES.md`의 "Todo.md 작업 추적" 섹션은 같은 개편에서
> **"12. CHANGELOG.md 작업 추적"** 으로 대체됐다(원문 보존을 위해 본문은 수정하지 않음).

---

# Todo (할일 목록)

> SauceLabs My Demo App Appium 자동화 프로젝트의 작업 추적
> 작성 규칙: `GIT_RULES.md` 섹션 "Todo.md 작업 추적" 참고

---

## 2026-07-01

### 오늘 완료 ✅
- ~~코드 리뷰 🟠 권장(Medium) 9/10 구현 (R-03·04·05·06·07·09·10·12·14)~~ ✅
- ~~검증: py_compile · `bash -n` · `pytest --collect-only` 29 tests~~ ✅
- ~~신규 브랜치 `fix/code-review-medium`에 커밋/푸시~~ ✅

### 남은 작업
- [ ] **R-11 (iOS 카트 수량 테스트)** 보류 해제 — 시뮬 부팅 후 `python tools/ui_dump_ios.py -w`로 수량/합계 accessibility id 확보 → iOS `CartPage` getter 보강 → `test_change_quantity` 추가
- [ ] **온디바이스 회귀** — Medium 9건 (특히 conftest 드라이버 리팩터·R-09·R-10·R-12) 실기기/에뮬 검증 후 main 병합

---

## 2026-06-30

### 오늘 완료 ✅
- ~~README 검토·보강 (구조 트리·문서 표 정정, 주요 특징·테스트 커버리지 섹션 추가)~~ ✅
- ~~README에 실행 구조 다이어그램 추가 (Mermaid 2종: 실행 파이프라인 + 코드 레이어)~~ ✅

---

## 2026-06-29

### 오늘 완료 ✅
- ~~원격 최신(`dc6208f`) fast-forward pull + 동기화~~ ✅
- ~~전체 코드 리뷰 (10영역 병렬 + 적대적 검증, 65건 도출)~~ ✅
- ~~Public 레포 보안 하드닝 A1~A3 적용 + 검증 + push~~ ✅
  - ~~A1: `.gitignore` bare `.env` 차단~~ / ~~A2: `serve.py` 127.0.0.1 바인딩~~ / ~~A3: `conftest.py` 대시보드 업로드 익명화~~

### 코드 리뷰 개선 작업 (2026-06-29 · 전체 65건)

> 상세 항목·체크리스트는 **`docs/CODE_REVIEW_2026-06-29.md`** 참고. 작업 시 그 문서의 체크박스를 갱신할 것.

- [x] 🔴🟠 보안 (A1~A3, 3건) — 완료, 커밋 `a82a154`
- [ ] 🟠 권장 (Medium) — **9/10 완료** (2026-07-01, 브랜치 `fix/code-review-medium`), R-11(iOS 카트 수량)만 보류 · `docs/CODE_REVIEW_2026-06-29.md` §4
- [ ] 🟡 개선 (Low 41건) — `docs/CODE_REVIEW_2026-06-29.md` §5
- [ ] ℹ️ 정보성 (Info 10건) — `docs/CODE_REVIEW_2026-06-29.md` §6
- [x] (선택) 레포명 정리 ✅ 2026-07-13 — `gh repo rename`으로 `appium_saucelabs`로 변경(remote URL 자동 갱신), README·셋업 가이드 참조 일괄 정정

---

## 2026-05-15

### 오늘 완료 ✅
- ~~**CLAUDE.md / .claude/ 공개 여부 결정** → 옵션 A 선택 (그대로 공개)~~ ✅
- ~~누적 변경사항 push 완료 (apk→apps, .gitignore, capabilities.py, CLAUDE.md, 메타파일)~~ ✅

---

## 진행 예정

### Step 8: 환경 구동 확인

- [x] venv 생성 + `pip install -r requirements.txt` ✅ 2026-06-20
- [x] `npm install` (Appium 3.5.2 + uiautomator2/xcuitest 드라이버) ✅
- [x] import 검증 (`from config.capabilities import ANDROID_CAPS`) ✅
- [x] pytest 수집 검증 (`pytest --collect-only` — conftest 정상 로드) ✅
- [x] Appium 서버 실행 (`npx appium`, 3.5.2 ready) ✅
- [x] 에뮬레이터 부팅 (`Pixel_8` → emulator-5554) ✅
- [x] 앱 설치·실행 확인 (스모크 세션에서 APK 자동 설치 + 카탈로그 노출) ✅

### Step 9: 첫 테스트 작성

- [x] UI Dump로 첫 화면 locator 확보 ✅ 2026-06-20
- [x] 가장 단순한 테스트 (앱 실행 → 카탈로그 첫 화면) — `tests/android/smoke_test.py`, 2 passed ✅
- [x] Allure 리포트 생성 확인 (`run_allure.py`) ✅
- [x] 시나리오 범위 결정 → 19개 기능영역/60+ 동작 인벤토리(`docs/APP_STRUCTURE.md`) ✅ 2026-06-20
- [x] 점진적 확장: 해피패스 E2E + Tier 1·2 (17 케이스 전부 통과) ✅ 2026-06-20

### Step 10: 자동화 확장 (다음)

- [ ] **Tier 3** (권한·디바이스 의존): QR(카메라)/Geo(위치)/FingerPrint(지문)/Drawing(제스처)/Virtual USB/Crash 2종
- [ ] 보강: 결제수단 필드 검증(F6.2), 다중 상품 구매, 정렬 name asc/desc, 다른 상품 구매 경로
- [x] iOS 포팅 — 탭바 POM(`pages/ios/`) + 10 테스트 통과 (서브에이전트 병렬 작성) ✅ 2026-06-20
- [ ] **iOS 키보드 블로커 해결** (Xcode 26.5 한글 소프트 키보드) → 해결 시 체크아웃 완주 E2E + WebView URL 추가 (현재 2건 skip)
- [ ] iOS 추가: 결제수단/리뷰/완료 화면(키보드 해결 후), QR·Geo·Drawing·Biometrics(권한·디바이스 의존)
- [x] change_notes/Todo 커밋 + push ✅ 2026-06-20 (커밋 `27d012a`)

### 코드 리뷰 개선 (2026-06-21 도출 · 14건)

> 다차원 코드 리뷰(서브에이전트 83개: 72 발견 → 57 적대적 검증 통과 → 14 종합)에서 도출. 기존 Step 10 항목과 중복 없음.

**P1 — 신뢰성·즉시 위험**
- [x] **BasePage stale-element 재시도 헬퍼** ✅ 2026-06-21 — `_retry_on_stale(action, retries=2)`로 `click`/`input_text`/`get_text`를 감싸 `StaleElementReferenceException` 시 locator 재탐색·재시도 (RecyclerView 재렌더·카트 수량변경 간헐 실패 방지)
- [x] **`tools/update_dashboard.py` SyntaxWarning 제거** ✅ 2026-06-21 — 라인389 `\/` → raw 문자열화(향후 SyntaxError로 대시보드 생성 중단 위험) + 미사용 함수/import 정리
- [x] **pytest 설정 단일화** ✅ 2026-06-21 — `pyproject.toml`의 `[tool.pytest.ini_options]` 삭제, `pytest.ini`를 정식 출처로(마커 5종 보존). "ignoring pytest config in pyproject.toml" 경고 해소

**P2 — 권장** (모두 온디바이스 회귀 검증 완료: Android 17 passed / iOS 10 passed·2 skip)
- [x] **E2E·수량 합계 assert 강화** ✅ 2026-06-21 — `_money()` 파서 + `합계 == round(상품가합 + 배송비 5.99, 2)` 검증, 카트 수량2배 → `t2 == round(t1*2, 2)` assert (Android `test_checkout_e2e`/`test_cart` 통과로 검증)
- [x] **색상·별점 선택 효과 검증** ✅ 2026-06-21 — iOS `is_color_selected()` 추가. ⚠️온디바이스 검증 중 `{color}ColorSelected` 이름 추측이 틀림을 발견 → 실제는 이름이 항상 `...ColorUnSelected`이고 **`selected=true` 속성**으로 선택 표현 → 속성 기반으로 정정 후 통과
- [x] **iOS 인덱스 의존 locator 교체** ✅ 2026-06-21 — 체크아웃 `FIELD_FULL_NAME…FIELD_COUNTRY` 의미 상수 + `EXPECTED_FIELD_COUNT` 가드, `rate_product(stars)`는 `name CONTAINS "Star"` class-chain + `len` 가드로 파라미터화
- [x] **리포트 후처리 단일화** ✅ 2026-06-21 — `run-app.sh` STEP4(LATEST/CSS/history/sed) 제거 → `run_allure.py`에 위임 (macOS BSD `sed -i ''` 버그 원천 제거)
- [x] **테스트 데이터·계정 중앙관리 모듈** ✅ 2026-06-21 — `config/test_data.py` 신설(계정·`SHIPPING`·`PAYMENT` 집약). Android=`bod@`/iOS=`bob@`는 앱 실제값이라 분리 유지(오타 아님)
- [~] **driver fixture 3중복 제거 + 플랫폼 판정 단일화** — 일부만 진행: `utils/helpers.is_android()` 추가(판정 단일화). `_build_driver` 공통화/미사용 fixture 정리는 **보류**(코어 인프라, 디바이스 검증 없이 리스크 대비 효익 낮음 → 별도 작업으로)

**P3 — 선택** (#10·#12는 온디바이스 검증 완료)
- [x] **타임아웃 매직넘버 통일** ✅ 2026-06-21 — `BasePage.SHORT_TIMEOUT=5` 상수, `is_invisible()` 헬퍼, `is_visible`/`get_cart_badge_count` 상수 사용, `hide_keyboard` 예외를 print로 안전화(삼킴 제거)
- [x] **마커 전략 완성** ✅ 2026-06-21 — `pytest.ini`에 `--strict-markers`, `smoke`/`regression`/`e2e` 실제 부착(smoke 6/regression 20/e2e 1 선택 확인), `PYTEST_GUIDE`에 `-m` 레시피 문서화
- [x] **iOS flows 모듈** ✅ 2026-06-21 — `utils/flows_ios.py` 신설(네비 5함수), `flows.login_as_valid_user` 추가, 양 플랫폼 명명 일치 (iOS 테스트 전부 flows_ios 사용·통과)
- [x] **문서 드리프트 정정** ✅ 2026-06-21 — `CODING_GUIDELINES`(실제 BasePage API), `IOS_TEST_GUIDE`(`implicitly_wait(10)` 제거), `CLAUDE.md`/`README`(구조·버전·UDID), `UI_DUMP_GUIDE`(예시 주의문) 정정
- [x] **`.env.example` 동기화** ✅ 2026-06-21 — `IOS_UDID`·`ANDROID_HOME` 추가 + `capabilities.py` docstring 보강

---

## 보류 / 차후 결정

- README 영문판 작성 (한국어판 완료, 영문은 추후)
- 서브 프로젝트: WebdriverIO Native Demo App (메인 완료 후)
- CI/CD 파이프라인 구축 (GitHub Actions)
- iOS 테스트 본격 작성 (집 macOS 환경 필요)

---

## ~~완료된 작업~~ ✅

### ~~2026-06-20: 이전 프로젝트 → SMDA 잔존 흔적 전면 정리~~ ✅
- ~~README 전면 재작성 + docs 11개 SMDA 단일환경화~~
- ~~`.claude/skills/mcp-scenario` 6개 정리 (`change_language` → `add_to_cart`)~~
- ~~MCP 도구/셸 단일환경 리팩터링 (깨진 `session_recorder.py` import 수정 포함)~~
- ~~다중환경·삭제 코드 참조 제거, grep 0건 검증~~
- ~~네이티브 Android 전환: `com.saucelabs.mydemoapp.android` + `apps/android/mda-2.2.0-25.apk` 배치 (RN→네이티브, APK 실측 일치 검증)~~

### ~~2026-05-15: 옵션 A 결정 + 누적 변경사항 push~~ ✅
- ~~CLAUDE.md / .claude/ 공개 결정 (옵션 A: 그대로 공개)~~
- ~~누적 변경사항 GitHub origin/main에 push~~

### ~~2026-05-14: Step 7 SauceLabs 앱 배치 + 폴더명 정리~~ ✅
- ~~`apk/` → `apps/` 폴더명 변경 (Android/iOS 통합)~~
- ~~`apps/android/` Android APK 배치~~
- ~~`apps/ios/` iOS .ipa(실기기) + .zip(시뮬레이터) 배치~~
- ~~`config/capabilities.py` 경로 수정 (3곳)~~
- ~~`CLAUDE.md` 경로 수정 (3곳)~~
- ~~`.gitignore` 정리 (Public 정책: apps/, *.apk, *.ipa 제외)~~

### ~~2026-05-14: Step 6 Git 초기화 + GitHub 원격 연결~~ ✅
- ~~`git init` + `git branch -M main`~~
- ~~GitHub `appium-saucelabs` 레포 생성 (Public)~~
- ~~첫 커밋 + push 완료~~

### ~~2026-05-14: Step 5 프로젝트 메타파일 작성~~ ✅
- ~~`change_notes.md` 신규 작성~~
- ~~`Todo.md` 신규 작성~~
- ~~`GIT_RULES.md` 정리 (이전 프로젝트 특화 제거, origin 단일 푸시 정책)~~
- ~~`CLAUDE.md` 정리 (이전 프로젝트 특화 제거, 활용 가능 내용 보존)~~

### ~~2026-05-14: Step 4 코드 정리 (이전 프로젝트 의존성 제거)~~ ✅
- ~~`conftest.py` 깨진 import 및 이전 프로젝트 fixture 제거~~
- ~~`config/capabilities.py` SauceLabs용 단순화~~
- ~~`.env.example` SauceLabs용 재작성~~
- ~~`.vscode/settings.json` Python 인터프리터 경로 정리~~

### ~~2026-05-14: Step 3 보안/Git 정리 (잔여)~~ ✅
- ~~`.git/` 폴더 삭제 (이전 프로젝트 원격 제거)~~

### ~~2026-05-13: Step 1-3 회사에서 진행~~ ✅
- ~~Step 1: 캐시/결과물 삭제 (10개 항목)~~
- ~~Step 2: 이전 프로젝트 코드/테스트/문서/APK 삭제 (34개 항목)~~
- ~~Step 3 일부: `.env` 파일 삭제~~
- ~~인수인계 문서 작성~~
